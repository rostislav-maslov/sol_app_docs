# Task List

Короткий рабочий список задач для AI. Одна строка в секции `Tasks` равна одной
маленькой задаче.

## Рабочий формат

После команды `отформатируй задачи` каждая задача должна выглядеть так:

```text
🔴 [<код>] Короткое описание задачи
```

Где:

- `🔴` - статус задачи.
- `<код>` - код задачи.
- детализация задачи хранится в файле `task-details/<код>.md`.

Допустимые примеры кода:

- `01-001` - локальный код репозитория;
- `PROJ-123` - Jira key;
- `B24-12345` - Bitrix task id в согласованном формате репозитория.

## Карта статусов

- `🔴` - новая задача.
- `🟠` - задача детализирована.
- `🟡` - задача в работе.
- `🟣` - AI завершил работу по задаче, но она еще ждет ручной проверки.
- `🟢` - задача вручную проверена пользователем и подтверждена как корректно выполненная.

Правило:

- после завершения своей работы AI переводит задачу в `🟣`;
- в `🟢` задачу переводит только пользователь после собственной проверки.

## Tasks

<!--
Добавляй новые задачи ниже этой секции по одной строке.
Можно писать сырой текст без кода и статуса.
После команды "отформатируй задачи" список должен быть приведен к рабочему формату.
Пример:
🔴 [01-001] Описать новую сущность EventConfig
-->
