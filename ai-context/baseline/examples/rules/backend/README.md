# Backend Rules Example

Это baseline-example контура `backend/` для `ai-context/workspace/rules/`.

В рабочем репозитории project-specific backend-правила должны жить в
`ai-context/workspace/rules/backend/`, а не в baseline.
