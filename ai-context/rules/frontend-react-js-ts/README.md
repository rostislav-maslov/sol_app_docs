# Frontend React/JS/TS Rules

Эта директория зарезервирована под project-specific правила для frontend на
`React`, `JavaScript` и `TypeScript`.

Сюда позже можно добавлять:

- правила структуры frontend-модулей;
- стандарты компонентов и страниц;
- правила state management и data fetching;
- UI naming и boundary-правила.

Пока это только каркас директории.
